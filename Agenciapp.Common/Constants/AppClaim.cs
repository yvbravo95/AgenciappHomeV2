namespace Agenciapp.Common.Constants
{
    public class AppClaim
    {
        public const string Token = nameof(Token);
        public const string TokenExpirationUtc = nameof(TokenExpirationUtc);
    }
}
